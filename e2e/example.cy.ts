describe('Example Test', () => {
  it('Visits the app root url', () => {
    cy.visit('/');
    // Add assertions here to check if the page loads correctly
    // For example: cy.contains('Welcome').should('be.visible');
  });
});
