# 🚀 دليل إعداد واتساب للبيئة الحقيقية (Production)

تم تحديث الكود ليعمل بطريقة آمنة ومناسبة للبيئة الحقيقية. هذا الدليل يشرح لك الخطوات النهائية المطلوبة لتفعيل حسابك ورقمك الحقيقي في Twilio.

## التغييرات التي تمت

1.  **تعزيز الأمان**: تم نقل مفاتيح Twilio السرية (Account SID و Auth Token) من الواجهة الأمامية (ملف `.env`) إلى بيئة الخادم الآمنة في Supabase.
2.  **كود جاهز للإنتاج**: لم يعد الكود يرسل أي مفاتيح سرية من المتصفح، مما يحمي حسابك تمامًا.

## ✅ خطوات الإعداد النهائية (مطلوبة منك)

الخطوة الأخيرة هي إضافة مفاتيح حسابك الحقيقي ورقمك الجديد في لوحة تحكم Supabase.

### 1. اذهب إلى لوحة تحكم Supabase

-   افتح مشروعك في [Supabase Dashboard](https://supabase.com/dashboard).

### 2. انتقل إلى إعدادات الوظيفة السحابية

-   من القائمة الجانبية، اختر `Edge Functions`.
-   اضغط على وظيفة `send-whatsapp`.

### 3. أضف المفاتيح السرية (Secrets)

-   داخل صفحة `send-whatsapp`، اضغط على قسم `Secrets`.
-   اضغط على `Add a new secret` وقم بإضافة **ثلاثة** متغيرات سرية بالأسماء والقيم التالية:

| اسم المتغير (Secret Name)     | القيمة (Value)                                                | الوصف                                      |
| ----------------------------- | ------------------------------------------------------------- | ------------------------------------------ |
| `TWILIO_ACCOUNT_SID`          | `ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`                             | الـ Account SID الخاص بحسابك في Twilio.     |
| `TWILIO_AUTH_TOKEN`           | `your_32_character_auth_token`                                | الـ Auth Token الخاص بحسابك في Twilio.      |
| `TWILIO_WHATSAPP_NUMBER`      | `whatsapp:+974...`                                            | رقم واتساب الحقيقي الذي حصلت عليه من Twilio. |

**ملاحظات هامة:**
-   تأكد من نسخ الأسماء (`Secret Name`) **تمامًا كما هي مكتوبة** (Case-sensitive).
-   استخدم رقمك الحقيقي في `TWILIO_WHATSAPP_NUMBER` بالصيغة الدولية الكاملة مسبوقًا بـ `whatsapp:`.

### 4. (اختياري) تنظيف ملف `.env`

-   لأن المفاتيح أصبحت الآن في الخادم، لم تعد هناك حاجة لوجودها في ملف `.env` المحلي.
-   يمكنك حذف الأسطر التالية من ملف `.env` لزيادة الأمان:
    ```env
    # Twilio WhatsApp Configuration
    VITE_TWILIO_ACCOUNT_SID=...
    VITE_TWILIO_AUTH_TOKEN=...
    VITE_TWILIO_WHATSAPP_NUMBER=...
    ```

## الخطوة الأخيرة: النشر (Deploy)

-   بعد إضافة المفاتيح السرية، يجب إعادة نشر الوظيفة السحابية لتطبيق التغييرات.
-   يمكنك فعل ذلك عبر Supabase CLI أو من خلال ربط حساب GitHub.

---

بمجرد إكمال هذه الخطوات، سيكون نظامك جاهزًا لإرسال رسائل واتساب من رقمك الحقيقي بأمان وكفاءة. 