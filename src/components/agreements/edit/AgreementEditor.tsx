
// Import directly from the badge component file to avoid circular references
import { Badge } from "@/components/ui/badge";
import { AgreementEditor as EditAgreementContent } from './EditAgreementContent';

// Export the AgreementEditor component from EditAgreementContent
export { AgreementEditor } from './EditAgreementContent';
